"""mapper_worker — LLM + vector-retrieval mapper.

Import ``build`` from :mod:`app.agents.workers.mapper_worker.graph` directly.
Kept empty here to avoid a circular import via ``deps`` at package-load time.
"""
