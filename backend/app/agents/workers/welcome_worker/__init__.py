"""welcome_worker — greet the user when the chat session connects.

Import ``build`` from :mod:`app.agents.workers.welcome_worker.graph` directly.
This package's ``__init__`` is deliberately empty to avoid circular imports.
"""
