"use client";

import { PipelineView } from "@/components/dashboard/pipeline-view";

/** Workspace wireframe scaffold — parked until dashboard work resumes. */
export default function FutureDashboardPage() {
  return <PipelineView />;
}
